# GitHub Pages는 반드시 'Deploy from a branch' → main → /docs 로 설정하세요.
# 로컬 미리보기: hugo server --baseURL "http://localhost:1313/"
baseURL: "https://myunbin.github.io/"
publishDir: docs
languageCode: "ko-kr"
title: "Researcher"

# 메인 페이지에 표시할 내용은 content/_index.md front matter에서 설정
params:
  description: "Researcher personal page"
  assetVersion: "2"
  footerName: "Hyunbin Kim"

markup:
  goldmark:
    renderer:
      unsafe: true
